{
  "name": "@a.shulman/text-feedback-extension",
  "version": "1.1.6",
  "description": "Diplodoc extension: floating feedback form for selected text",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "exports": {
    ".": {
      "types": "./build/index.d.ts",
      "default": "./build/index.js"
    }
  },
  "homepage": "https://docs.ideco.ru/",
  "engines": {
    "node": ">=22"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/a-shulman/text-feedback.git"
  },
  "bugs": {
    "url": "https://github.com/a-shulman/text-feedback/issues"
  },
  "author": "a-shulman",
  "license": "MIT",
  "files": [
    "build"
  ],
  "scripts": {
    "build": "run-p build:*",
    "build:clean": "rm -rf build",
    "build:js": "./esbuild/build.mjs",
    "build:declarations": "tsc --project tsconfig.publish.json --emitDeclarationOnly --outDir ./build",
    "typecheck": "tsc --noEmit && tsc --project src/browser/tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "@diplodoc/cli": ">=5"
  },
  "devDependencies": {
    "@diplodoc/cli": "5.52.0",
    "@diplodoc/tsconfig": "^1.0.2",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "esbuild": "^0.28.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.5.3",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@a.shulman/text-feedback-extension": "^1.1.2"
  }
}
